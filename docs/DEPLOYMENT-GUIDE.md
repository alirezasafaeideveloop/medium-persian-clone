# 🚀 راهنمای کامل استقرار واقعی وبسایت

این راهنما شما را قدم به قدم برای استقرار واقعی وبسایت مدیوم فارسی در اینترنت هدایت می‌کند.

## 📋 پیش‌نیازهای اولیه

### ۱. دانش فنی مورد نیاز
- آشنایی مقدماتی با لینوکس (برای سرور)
- دانش پایه‌ای از Git و GitHub
- آشنایی با خط فرمان (Terminal/Command Line)

### ۲. ابزارهای مورد نیاز
- کامپیوتر با سیستم‌عامل ویندوز، مک یا لینوکس
- اتصال اینترنت پایدار
- کارت اعتباری برای پرداخت‌های آنلاین (اختیاری)

---

## 🎯 مسیرهای استقرار

من ۳ مسیر اصلی برای استقرار وبسایت شما توضیح می‌دهم:

### 🌟 مسیر ۱: رایگان و آسان (توصیه شده برای شروع)
**پلتفرم‌های:**
- **Vercel** (بهترین برای Next.js)
- **Netlify** (رایگان و ساده)
- **Cloudflare Pages** (رایگان و سریع)

### 💰 مسیر ۲: ارزان و کنترل کامل
**پلتفرم‌ها:**
- **DigitalOcean** ($6/ماهانه)
- **Linode** ($5/ماهانه)
- **Vultr** ($6/ماهانه)

### 🏢 مسیر ۳: حرفه‌ای و قدرتمند
**پلتفرم‌ها:**
- **AWS EC2** (از $3.50/ماهانه)
- **Google Cloud** (از $4.28/ماهانه)
- **Microsoft Azure** (از $13/ماهانه)

---

## 🌟 مسیر ۱: استقرار رایگان با Vercel (توصیه شده)

### مرحله ۱: آماده‌سازی پروژه
```bash
# ۱. پروژه را به GitHub بفرستید
git add .
git commit -m "Initial commit: Persian Medium Clone"
git branch -M main
git remote add origin https://github.com/username/medium-persian-clone.git
git push -u origin main

# ۲. مطمئن شوید فایل‌های زیر وجود دارند:
# - .env.production.example
# - package.json
# - next.config.js
```

### مرحله ۲: ثبت‌نام در Vercel
1. به [vercel.com](https://vercel.com) بروید
2. با GitHub ثبت‌نام کنید (رایگان است)
3. روی "New Project" کلیک کنید

### مرحله ۳: اتصال GitHub
1. ریپازیتوری GitHub خود را انتخاب کنید
2. Vercel به طور خودکار پروژه Next.js را تشخیص می‌دهد
3. روی "Deploy" کلیک کنید

### مرحله ۴: پیکربندی متغیرهای محیطی
در داشبورد Vercel:
1. به بخش "Settings" → "Environment Variables" بروید
2. متغیرهای زیر را اضافه کنید:
```
NODE_ENV=production
NEXTAUTH_SECRET=your-random-secret-key-here
NEXTAUTH_URL=https://your-project-name.vercel.app
```

### مرحله ۵: استقرار خودکار
Vercel به طور خودکار پروژه شما را استقرار می‌دهد و یک URL به شما می‌دهد:
```
https://your-project-name.vercel.app
```

### مزایا:
- ✅ کاملاً رایگان
- ✅ SSL خودکار
- ✅ CDN جهانی
- ✅ استقرار با یک کلیک
- ✅ بهینه برای Next.js

---

## 💰 مسیر ۲: استقرار با DigitalOcean ($6/ماهانه)

### مرحله ۱: ایجاد حساب کاربری
1. به [digitalocean.com](https://www.digitalocean.com) بروید
2. ثبت‌نام کنید (ایمیل و کارت اعتباری لازم است)
3. $10 شارژ اولیه را پرداخت کنید

### مرحله ۲: ایجاد Droplet (سرور مجازی)
1. به داشبورد بروید → "Create" → "Droplets"
2. تنظیمات زیر را انتخاب کنید:
   - **Image**: Ubuntu 22.04 LTS
   - **Plan**: Basic ($6/ماهانه)
   - **Region**: نزدیک‌ترین به ایران (معمولاً Frankfurt)
   - **Size**: 1 GB RAM, 1 CPU, 25 GB SSD
3. نام سرور خود را بگذارید (مثلاً `medium-persian`)
4. "Create Droplet" را بزنید

### مرحله ۳: اتصال به سرور
پس از ایجاد Droplet، ایمیل حاوی IP و دسترسی را دریافت می‌کنید:

**در ویندوز (با PowerShell):**
```powershell
ssh root@YOUR_SERVER_IP
```

**در مک/لینوکس:**
```bash
ssh root@YOUR_SERVER_IP
```

### مرحله ۴: نصب پیش‌نیازها روی سرور
```bash
# آپدیت سیستم
apt update && apt upgrade -y

# نصب Node.js 18
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
apt-get install -y nodejs

# نصب Git
apt install -y git

# نصب Docker (اختیاری اما توصیه شده)
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh

# نصب Docker Compose
curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
chmod +x /usr/local/bin/docker-compose
```

### مرحله ۵: استقرار پروژه
```bash
# رفتن به home
cd ~

# کلون پروژه از GitHub
git clone https://github.com/username/medium-persian-clone.git

# ورود به پوشه پروژه
cd medium-persian-clone

# نصب وابستگی‌ها
npm install

# پیکربندی متغیرهای محیطی
cp .env.production.example .env.production
nano .env.production  # ویرایش فایل
```

### مرحله ۶: اجرا با Docker
```bash
# ساخت و اجرا
docker-compose -f docker-compose.prod.yml up -d

# مشاهده لاگ‌ها
docker-compose -f docker-compose.prod.yml logs -f
```

### مرحله ۷: پیکربندی دامنه (اختیاری)
اگر دامنه دارید:
1. DNS دامنه خود را به IP سرور指向 کنید:
   ```
   Type: A
   Name: @
   Value: YOUR_SERVER_IP
   ```
2. در فایل `nginx.conf`، دامنه خود را جایگزین کنید:
   ```nginx
   server_name your-domain.com www.your-domain.com;
   ```

---

## 🏢 مسیر ۳: استقرار با AWS (حرفه‌ای)

### مرحله ۱: ایجاد حساب AWS
1. به [aws.amazon.com](https://aws.amazon.com) بروید
2. "Create a Free Account" را بزنید
3. اطلاعات خود را کامل کنید (نیاز به کارت اعتباری دارد)

### مرحله ۲: ایجاد EC2 Instance
1. به کنسول AWS بروید → Services → EC2
2. "Launch Instance" را بزنید
3. تنظیمات زیر را انتخاب کنید:
   - **Name**: medium-persian-server
   - **AMI**: Ubuntu Server 22.04 LTS
   - **Instance Type**: t2.micro (رایگان برای 12 ماه)
   - **Key Pair**: یک کلید جدید ایجاد و دانلود کنید
4. "Launch Instance" را بزنید

### مرحله ۳: اتصال به سرور AWS
```bash
# تغییر دسترسی فایل کلید
chmod 400 your-key-name.pem

# اتصال به سرور
ssh -i your-key-name.pem ubuntu@YOUR_SERVER_IP
```

### مرحله ۴ تا ۷: مانند مسیر DigitalOcean
مراحل ۴ تا ۷ مشابه مسیر DigitalOcean است.

---

## 🔧 پیکربندی متغیرهای محیطی

برای هر سه مسیر، باید متغیرهای زیر را تنظیم کنید:

### متغیرهای ضروری:
```env
# تولید کلید تصادفی:
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

NEXTAUTH_SECRET=your-generated-secret-key-here
NEXTAUTH_URL=https://your-domain.com
NODE_ENV=production
```

### متغیرهای اختیاری:
```env
# دیتابیس (اگر PostgreSQL می‌خواهید):
DATABASE_URL=postgresql://username:password@localhost:5432/medium_farsi

# OAuth (برای ورود با گوگل/گیت‌هاب):
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# ایمیل (برای ریست پسورد):
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
```

---

## 🌐 خرید و پیکربندی دامنه

### ۱. خرید دامنه
**سرویس‌های ایرانی:**
- **iranserver.com** (از ۲۰,۰۰۰ تومان)
- **mihanwebhost.com** (از ۱۵,۰۰۰ تومان)
- **parspack.com** (از ۱۸,۰۰۰ تومان)

**سرویس‌های خارجی:**
- **namecheap.com** (از $9/سال)
- **godaddy.com** (از $12/سال)
- **cloudflare.com** (رایگان برای DNS)

### ۲. پیکربندی DNS
پس از خرید دامنه:
1. به پنل DNS بروید
2. رکورد A ایجاد کنید:
   - **Type**: A
   - **Name**: @
   - **Value**: IP سرور شما
   - **TTL**: 3600

---

## 🔒 SSL Certificate (رایگان)

### با Let's Encrypt:
```bash
# نصب Certbot
sudo apt install certbot python3-certbot-nginx

# دریافت گواهی SSL
sudo certbot --nginx -d your-domain.com -d www.your-domain.com

# تمدید خودکار (اختیاری)
sudo crontab -e
# این خط را اضافه کنید:
0 12 * * * /usr/bin/certbot renew --quiet
```

---

## 📊 هزینه‌ها

### مسیر رایگان (Vercel):
- **هزینه**: $0
- **دامنه**: اختیاری
- **SSL**: رایگان
- **پهنای باند**: نامحدود

### مسیر ارزان (DigitalOcean):
- **سرور**: $6/ماهانه
- **دامنه**: $10-15/سالانه
- **SSL**: رایگان (Let's Encrypt)
- **مجموع**: ~$80-100/سالانه

### مسیر حرفه‌ای (AWS):
- **سرور**: $3.50/ماهانه (t2.micro رایگان 12 ماه)
- **دامنه**: $10-15/سالانه
- **SSL**: رایگان
- **مجموع**: ~$50-70/سالانه (اول سال)

---

## 🚀 شروع سریع (برای عجول‌ها)

### سریع‌ترین راه:
1. **همین الان** به [vercel.com](https://vercel.com) بروید
2. با GitHub لاگین کنید
3. ریپازیتوری خود را Import کنید
4. Deploy را بزنید
5. بعد از ۲-۳ دقیقه وبسایت شما آنلاین است!

### URL نهایی:
```
https://your-project-name.vercel.app
```

---

## 🛠️ عیب‌یابی رایج

### مشکل: Build fails
```bash
# بررسی نسخه Node.js
node --version  # باید 18+ باشد

# پاک کردن کش
npm cache clean --force
rm -rf node_modules package-lock.json
npm install
```

### مشکل: Database connection
```bash
# بررسی اتصال دیتابیس
npx prisma db pull

# ری‌ست دیتابیس
npx prisma migrate reset
```

### مشکل: Port already in use
```bash
# پیدا کردن فرآیند روی پورت 3000
lsof -i :3000

# کشتن فرآیند
kill -9 PID
```

---

## 📞 پشتیبانی

اگر در هر مرحله‌ای به مشکل برخوردید:
1. **مستندات پروژه** را مطالعه کنید
2. **GitHub Issues** ایجاد کنید
3. **Stack Overflow** جستجو کنید
4. **Discord/Telegram** به گروه‌های توسعه‌دهندگان بپیوندید

---

## 🎉 تبریک!

پس از طی این مراحل، شما یک وبسایت کاملاً کاربردی مدیوم فارسی خواهید داشت که:
- ✅ آنلاین و در دسترس جهانی
- ✅ با SSL امن
- ✅ بهینه برای موتورهای جستجو
- ✅ با پشتیبانی کامل فارسی و RTL
- ✅ با تمام ویژگی‌های مدرن

موفق باشید! 🚀